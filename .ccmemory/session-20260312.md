---
name: Session 2026-03-12
description: Proxmox plugin fix, VMware networking, FreeBSD spec, kickstart hostname fix
type: project
---

## Session 2026-03-12 Summary

### Proxmox Plugin — RESOLVED
- Built patched `packer-plugin-proxmox` v1.2.3 from https://github.com/sshoecraft/packer-plugin-proxmox (PR #283 merged)
- `skip_convert_to_template` defaults to true — do NOT add it to proxmox.json config
- `_post_build` re-enabled as `post_build` in proxmox.json
- `proxmox_post_build.py` simplified to just rename VM from build_id to actual name (no more template/disk fixing)
- Docs updated: `docs/installation.md` (Proxmox Plugin section), `docs/reference/platform-reference.md` (important admonition)

### VMware Workstation Networking Fix
- Kernel updated to 6.8.0-101-generic, VMware modules recompiled
- ESXi VM (esxhost) lost network — `ethernet0` had no `connectionType` in VMX
- Fixed: added `ethernet0.connectionType = "custom"` and `ethernet0.vnet = "/dev/vmnet0"` to esxhost.vmx
- ESXi firewall `default action: DROP` was blocking traffic — user disabled it
- ESXi took several minutes to fully initialize after cold start

### ESXi NFS Datastore
- ESXi had no `vms` datastore — previous one was gone
- Created NFS export: `/home/steve/vms/vsphere *(rw,sync,no_subtree_check,no_root_squash)` in /etc/exports
- User mounts on ESXi: `esxcli storage nfs add -H 192.168.120.1 -s /home/steve/vms/vsphere -v vms`

### vSphere/esx Location
- Added `vsphere/esx` secret entry (server=192.168.1.251, root, P@55w0rd)
- Location `esx.json` targets ESXi at 192.168.1.251, datacenter=ha-datacenter, datastore=vms

### FreeBSD Spec Fixes
- Boot command: added cd2 fallback (`cd2 || cd1 || cd0`) for vSphere CD device ordering
- **installerconfig rewritten**: network brought up in chroot before pkg; `sudo` and `python3` installed during OS install for all platforms; `open-vm-tools-nox11` installed conditionally for vmware/vsphere
- Removed pre_provisioners from spec.json (pkg bootstrap/python3 now in installerconfig)
- FreeBSD base system does NOT include pkg/python/sudo — must be installed from network repos

### Kickstart Hostname Fix
- `--hostname=>>fqdn<<` was inside the static IP conditional — DHCP builds got `localhost`
- Fixed kickstart_5.cfg through kickstart_9.cfg: moved `--hostname` outside the conditional
- kickstart_3.cfg and kickstart_4.cfg were already correct

### Alma 9 on vSphere
- Soft lockup warnings during build on nested ESXi — expected in nested virtualization, not fatal
- Suppress with `nmi_watchdog=0` boot parameter if needed

### Still Pending
- QEMU alma-9.7 build: kernel crash on console — untested, may be related to machine type or other QEMU config
- Version bump to 1.7.0 still pending
- Spec merging bug (top-level arch_specific overrides version_specific arch_specific) still unfixed in core.py
- `~steve` directory mystery on freshly built VMs still unsolved
- pve location has vm_storage_pool="local" — should be "vms"
