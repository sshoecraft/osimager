# Session 2026-03-07: QEMU Platform Overhaul

## Changes Made

### 1. Removed libvirt platform
- Deleted `osimager/data/platforms/libvirt.json` (source, build, installed)
- Removed from SUPPORTED_PLATFORMS in constants.py
- Removed from all spec platforms arrays (rhel, ubuntu, debian, sles, mxlinux, xcpng, proxmox-ve)
- Updated all docs (README, platform-reference, defs-reference, supported-os, spec-reference, etc.)
- Platform count: 13 → 12

### 2. Removed all.json, moved defaults to config
- Deleted `osimager/data/platforms/all.json` (source, build, installed)
- Removed `"include": "all"` from all 10 platform JSON files
- Added to core.py default settings: cpu_sockets=1, cpu_cores=2, memory=2048, boot_disk_size=16384
- Added to constants.py DEFAULT_SETTINGS
- Added to save_keys in save_settings() so they persist to config.json
- Updated cli.py --list-defs and --list-platforms
- Updated docs/generate.py
- Updated all doc files referencing all.json
- Version bumped to 1.6.0

### 3. Ubuntu 24.04 point releases
- Changed versions from "24.04" to "24.04.[2-3]"
- Parent version_specific "24.04.*" uses `>>version<<` in URL template
- URLs: `https://releases.ubuntu.com/>>version<</ubuntu->>version<<-live-server-amd64.iso`
- Local ISO available: ubuntu-24.04.2-live-server-amd64.iso

### 4. QEMU platform fixes
- Replaced placeholder `http_directory: "path/to/httpdir"` with `cd_files`/`cd_label`
- Added KVM auto-detection: `"accelerator": "E>'kvm' if os.access('/dev/kvm', os.W_OK) else 'none'<E"`
- Added `"headless": true` default
- Bridge networking via location platform_specific `net_bridge`

### 5. Lab location changes
- vms_path: `/vms` → `/home/steve/vms`
- Added qemu platform_specific with net_bridge=br0, vnc_bind_address=0.0.0.0

### 6. System setup
- Installed qemu-system-x86, qemu-utils
- Created /etc/qemu/bridge.conf (allow br0)
- Set setuid on qemu-bridge-helper
- Removed steve from kvm group (not needed with auto-detect fallback)

## Outstanding Issues for Next Session
1. **cpu_sockets expression error**: `#>cpu_sockets<#` in qemu.json fails — the numeric expression can't resolve the key. cpu_sockets defaults to 1 in core.py settings and should flow into defs via `self.defs.update(self.settings)`. Debug the timing of when settings merge into defs vs when platform config expressions are evaluated. The `sockets` config key gets removed as empty value when this fails.
2. **Ubuntu network-config=disabled**: Boot command disables network config in autoinstaller. VM won't get IP on bridge. Need to either remove this or provide proper network config.
3. **KVM access**: User not in kvm group, so accelerator=none. Builds extremely slow. Consider making kvm group addition part of setup docs, or find alternative.
4. **End-to-end QEMU build not yet successful** — need to fix above issues and complete a full build.
